version: '3.8'

services:
  aim-academy-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aim-academy-frontend
    ports:
      - "9010:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
